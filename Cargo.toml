[package]
name = "memreduct"
version = "0.1.0"
edition = "2021"
description = "Rust rewrite of Henry++ Mem Reduct for Linux"
license = "GPL-3.0-only"

[dependencies]
anyhow = "1"
clap = { version = "4.5", features = ["derive"] }
libc = "0.2"
